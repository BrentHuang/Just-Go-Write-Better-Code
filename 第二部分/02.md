# 布尔类型

`bool` 是 Go 的内置基本类型，取值为 `true` 或 `false`。`true` 和 `false` 是布尔字面量，也是无类型常量。

布尔值不会隐式转换为 `int` 类型的 0 或 1，反之亦然。

用 `%t` 格式化输出布尔值：

```go
func main() {
 b := false
 fmt.Printf("%t\n", b)  // false
}
```
